pymongo>=4.6
# urllib3 v2 dropped urllib3.contrib.appengine; some older libs still import it — cap until those are upgraded.
urllib3>=1.26,<2
requests>=2.31
# scripts/enrich_cards_vinted.py (Vinted Selenium enrichment)
beautifulsoup4>=4.12
selenium>=4.15
webdriver-manager>=4.0
